RELAZIONE
-dispositivi stessa rete: dispositivo sorgente invia pacchetti allo switch, il quale li inoltra ad ogni dispositivo della rete.se dispositivo destinatario, oggetto della richiesta, è presente allora risponde rinviando i pacchetti al dispositivo sorgente (sempre tramite switch). I dispositivi comunicano con lo switch utilizzando indirizzo MAC.
-dispositivi reti differenti: dispositivo sorgente invia pacchetti allo switch0. Lo switch0 comunica con il router, il quale instrada i pacchetti allo switch1 (il router si mette nei panni della sorgente, quindi indirizzo MAC nei pacchetti viene sostituito con quello del router, nella seconda rete).Lo switch1 inoltra i pacchetti a tutti i dispositivi della rete, il destinatario risponde inviando i pacchetti indietro (destinatario->switch1->router[cambio indirizzo MAC]->switch0->sorgente)